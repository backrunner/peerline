# Agents

- Commit messages must follow the `xxx: desc` pattern, for example `docs: refresh README` or `chore: prep alpha release`.
- Keep commits small, focused, and grouped by concern.
- Use `BackRunner <dev@backrunner.top>` as the commit identity for release work in this repository.
